
import 'package:flutter/material.dart';

class DialogEditMenu extends StatelessWidget {
  const DialogEditMenu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
